var Reading = (function () {
    function Reading(element, settings) {
        this.element = element;
        this.settings = settings;
        this.modal = $('#textModal');
    }
    return Reading;
})();
