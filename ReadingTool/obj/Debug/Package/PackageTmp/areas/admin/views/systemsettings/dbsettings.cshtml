@model string
           
@{
    ViewBag.Title = "Index";
}

@section submenu { @Html.Partial("_menu") }

<p class="warning">
    These settings are from the database. <strong>They may not be the same as your current site settings!</strong> This can lead to unintended 
    consequences if they are currently different to the cached settings when they are reloaded, so it's important they are correct. You can 
    force a reload of the settings from the database by click the <strong>reload settings</strong> button in the menu.
</p>

<pre style="margin-left: 20px">@Model</pre>